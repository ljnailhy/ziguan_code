# <span style="color:red;">请严格按顺序执行，防止报错、重发！</span>

## 1. nacos配置
- asset-service
```yml

```

## 2. SQL

<details>
  <summary>点击展开SQL详情</summary>

```sql
use platform_asset;

ALTER TABLE `platform_asset`.`contract_info`
    MODIFY COLUMN `agent_way` bigint(0) NULL COMMENT '代理方式' AFTER `contract_abstract`;
ALTER TABLE `platform_asset`.`asset_transfer`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `remark`;
ALTER TABLE `platform_asset`.`allocation_info`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `allocation_illustrate`;
ALTER TABLE `platform_asset`.`lease_info`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `lease_purpose`;
ALTER TABLE `platform_asset`.`operation_info`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `remark`;
ALTER TABLE `platform_asset`.`project_transfer`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `transfer_illustrate`;
ALTER TABLE `platform_asset`.`recovery_adjust_trial`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `special_remarks`;
ALTER TABLE `platform_asset`.`recovery_execute`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `execute_ruling_issuance_time`;
ALTER TABLE `platform_asset`.`recovery_judgement`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `law_firm_id`;
ALTER TABLE `platform_asset`.`recovery_litigation_details`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `details_description`;
ALTER TABLE `platform_asset`.`recovery_payment`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `pay_explain`;
ALTER TABLE `platform_asset`.`recovery_payment_collection`
    ADD COLUMN `flow_state` enum('running','completed','terminated','canceled','draft') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT 'draft' COMMENT '流程状态' AFTER `is_collection_historical`;

```

</details>

## 3. 发业务包
- 后端asset-service.jar

## 4. 数据字典


## 5. 流程配置
### 流程
-

## 6. 重启流程引擎


## 95. 其他
- asset-service/file/template/资产导入模板.xls
- 

## 96. Nginx配置
```nginx
```

## 97. 系统菜单配置

## 98. 定时任务

## 99. 验证
- 测试（生产不可测流程）
- 代码合到master，并打tag

## 100. 发布说明

新增：
1. XXX

实现中：
1. XXX

优化：
1. XXX

修复：
1. XXX
