# <span style="color:red;">请严格按顺序执行，防止报错、重发！</span>

## 1. nacos配置
- asset-service
```yml

```

## 2. SQL

<details>
  <summary>点击展开SQL详情</summary>

```sql
use platform_asset;

ALTER TABLE `platform_asset`.`recovery_litigation`
    ADD COLUMN `is_submit` bit(1) NULL DEFAULT b'0' COMMENT '是否提交是1 否0' AFTER `is_stock`;
ALTER TABLE `platform_asset`.`allocation_info`
    ADD COLUMN `title` varchar(500) NULL COMMENT '标题' AFTER `tenant_id`;
ALTER TABLE `platform_asset`.`project_info_ext`
    CHANGE COLUMN `a_id` `aid` bigint(0) NULL DEFAULT NULL COMMENT 'a角' AFTER `risk_compensation`,
    CHANGE COLUMN `b_id` `bid` bigint(0) NULL DEFAULT NULL COMMENT 'b角' AFTER `acode`;
CREATE TABLE `manage_history_record` (
                                         `id` bigint NOT NULL COMMENT '主键ID',
                                         `tenant_id` bigint NOT NULL COMMENT '租户Id',
                                         `project_id` bigint DEFAULT NULL COMMENT '项目id',
                                         `manage` bigint DEFAULT NULL COMMENT '保全经理',
                                         `start_date` datetime DEFAULT NULL COMMENT '开始时间',
                                         `end_date` datetime DEFAULT NULL COMMENT '结束时间',
                                         `creator` bigint NOT NULL COMMENT '创建人',
                                         `create_stamp` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                                         `last_editor` bigint NOT NULL COMMENT '最后修改人',
                                         `last_edit_stamp` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后修改时间',
                                         PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='保全经理历史变更记录';
ALTER TABLE `platform_asset`.`asset_income_distribution`
    ADD COLUMN `cost_type` bigint NULL COMMENT '费用类型' AFTER `property_type`;
```

</details>

## 3. 发业务包
- 后端asset-service.jar

## 4. 数据字典


## 5. 流程配置
### 流程
-

## 6. 重启流程引擎


## 95. 其他
- asset-service/file/template/资产导入模板.xls
- 

## 96. Nginx配置
```nginx
```

## 97. 系统菜单配置
- 追偿项目 -> 项目管理 新增 proceedingProjectInfo.edit按钮

## 98. 定时任务

## 99. 验证
- 测试（生产不可测流程）
- 代码合到master，并打tag

## 100. 发布说明

新增：
1. XXX

实现中：
1. XXX

优化：
1. XXX

修复：
1. XXX
