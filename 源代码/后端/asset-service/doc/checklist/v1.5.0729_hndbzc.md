# <span style="color:red;">请严格按顺序执行，防止报错、重发！</span>

## 1. nacos配置
- asset-service
```yml

```

## 2. SQL

<details>
  <summary>点击展开SQL详情</summary>

```sql
use platform_asset;

ALTER TABLE `platform_asset`.`reve_property_info`
    ADD COLUMN `preserve_type` enum('BEFORE_LITIGATION_PRESERVE','MIDDLE_LITIGATION_PRESERVE') NULL COMMENT '保全类型\'BEFORE_LITIGATION_PRESERVE\'：诉前保全,\'MIDDLE_LITIGATION_PRESERVE\'：诉中保全' AFTER `debt_repayment_money`,
    ADD COLUMN `preserve_apply_date` datetime NULL COMMENT '保全申请提交时间' AFTER `preserve_type`,
    ADD COLUMN `preserve_rule_date` datetime NULL COMMENT '保全裁定送达时间' AFTER `preserve_apply_date`,
    ADD COLUMN `insure_money` decimal(18, 6) NULL COMMENT '保险费' AFTER `preserve_rule_date`;
ALTER TABLE `platform_asset`.`recovery_judgement`
    ADD COLUMN `judge_phone` varchar(255) NULL COMMENT '法官联系方式' AFTER `judge_name`;
```

</details>

## 3. 发业务包
- 后端asset-service.jar

## 4. 数据字典


## 5. 流程配置
### 流程
-

## 6. 重启流程引擎


## 95. 其他
- asset-service/file/template/资产导入模板.xls
- 

## 96. Nginx配置
```nginx
```

## 97. 系统菜单配置

## 98. 定时任务

## 99. 验证
- 测试（生产不可测流程）
- 代码合到master，并打tag

## 100. 发布说明

新增：
1. XXX

实现中：
1. XXX

优化：
1. XXX

修复：
1. XXX
