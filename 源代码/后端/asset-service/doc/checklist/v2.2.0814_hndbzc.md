# <span style="color:red;">请严格按顺序执行，防止报错、重发！</span>

## 1. nacos配置
- asset-service
```yml

```

## 2. SQL

<details>
  <summary>点击展开SQL详情</summary>

```sql
use platform_asset;

ALTER TABLE `platform_asset`.`property_info`
    CHANGE COLUMN `Taxe_fee` `taxe_fee` decimal (18, 6) NULL DEFAULT NULL COMMENT '税费/获得资产支付税费' AFTER `disposal_price`,
    ADD COLUMN `dispose_fee` decimal (18, 6) NULL COMMENT '处置资产支付税费' AFTER `taxe_fee`,
    MODIFY COLUMN `original_obligor_fee` decimal (18, 6) NULL DEFAULT NULL COMMENT '原债务人费用/获得资产支付的其他费用' AFTER `taxe_fee`;
ALTER TABLE `platform_asset`.`lease_info`
    MODIFY COLUMN `estimate_date` datetime(0) NULL DEFAULT NULL COMMENT '评估有效期开始时间' AFTER `estimate_money`,
    ADD COLUMN `estimate_end_date` datetime(0) NULL COMMENT '评估有效期截止时间' AFTER `estimate_date`;
ALTER TABLE `platform_asset`.`lease_info`
    ADD COLUMN `remark` varchar(2000) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT NULL COMMENT '备注' AFTER `flow_state`;
ALTER TABLE `platform_asset`.`asset_transfer`
    ADD COLUMN `agency_fee` decimal(18, 6) NULL COMMENT '应付代理费用' AFTER `estimate_end_date`;

```

</details>

## 3. 发业务包
- 后端asset-service.jar
- 流程引擎.jar

## 4. 数据字典


## 5. 流程配置
### 流程
- 

## 6. 重启流程引擎


## 95. 其他
- 

## 96. Nginx配置
```nginx

```

## 97. 系统菜单配置

## 98. 定时任务

## 99. 验证
- 测试（生产不可测流程）
- 代码合到master，并打tag

## 100. 发布说明

新增：
1. XXX

实现中：
1. XXX

优化：
1. XXX

修复：
1. XXX
