# <span style="color:red;">请严格按顺序执行，防止报错、重发！</span>

## 1. nacos配置
- asset-service
```yml
# 自定义开启配置
enabled: 
  minio: true
  xxlJob: true
```

## 2. SQL

<details>
  <summary>点击展开SQL详情</summary>

```sql
use platform_asset;

ALTER TABLE `platform_asset`.`asset_transfer`
    MODIFY COLUMN `deal_date` datetime(18) NULL DEFAULT NULL COMMENT '成交时间' AFTER `deal_money`;

CREATE TABLE `project_transfer` (
                                    `id` bigint NOT NULL COMMENT '主键ID',
                                    `tenant_id` bigint NOT NULL COMMENT '租户Id',
                                    `transfer_illustrate` text CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci COMMENT '移交说明',
                                    `is_deleted` bit(1) NOT NULL DEFAULT b'0' COMMENT '是否删除',
                                    `creator` bigint NOT NULL COMMENT '创建人',
                                    `create_stamp` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                                    `last_editor` bigint NOT NULL COMMENT '最后修改人',
                                    `last_edit_stamp` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后修改时间',
                                    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='项目移交';

CREATE TABLE `project_transfer_detailed` (
                                             `id` bigint NOT NULL COMMENT '主键ID',
                                             `tenant_id` bigint NOT NULL COMMENT '租户Id',
                                             `transfer_id` bigint DEFAULT NULL COMMENT '项目移交id',
                                             `project_id` bigint DEFAULT NULL COMMENT '项目id',
                                             `compensation_money` decimal(18,6) DEFAULT NULL COMMENT '代偿金额',
                                             `compensation_date` datetime DEFAULT NULL COMMENT '代偿时间',
                                             `manage` varchar(50) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci DEFAULT NULL COMMENT '保全经理',
                                             `law_firm_id` bigint DEFAULT NULL COMMENT '律所',
                                             `contract_id` bigint DEFAULT NULL COMMENT '关联合同',
                                             `collection_amount` decimal(18,6) DEFAULT NULL COMMENT '回款金额',
                                             `residue_compensation` decimal(18,6) DEFAULT NULL COMMENT '剩余代偿金额',
                                             `primary_affiliated_org` bigint DEFAULT NULL COMMENT '原项目归属方',
                                             `affiliated_org` bigint DEFAULT NULL COMMENT '现在项目归属方',
                                             `is_deleted` bit(1) NOT NULL DEFAULT b'0' COMMENT '是否删除',
                                             `creator` bigint NOT NULL COMMENT '创建人',
                                             `create_stamp` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
                                             `last_editor` bigint NOT NULL COMMENT '最后修改人',
                                             `last_edit_stamp` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后修改时间',
                                             PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci COMMENT='项目移交明细';

ALTER TABLE `platform_asset`.`asset_transfer`
    ADD COLUMN `is_again_auction` bit(1) NULL DEFAULT b'0' COMMENT '是否二次挂网' AFTER `programme`;
ALTER TABLE `platform_asset`.`law_firm_info`
    MODIFY COLUMN `org_id` bigint(0) NULL COMMENT '组织id' AFTER `tenant_id`;
```

</details>

## 3. 发业务包
- 后端asset-service.jar

## 4. 数据字典


## 5. 流程配置
### 流程

## 6. 重启流程引擎


## 95. 其他
- asset-service/file/template/资产导入模板.xls

## 96. Nginx配置
```nginx
```

## 97. 系统菜单配置


## 98. 定时任务

## 99. 验证
- 测试（生产不可测流程）
- 代码合到master，并打tag

## 100. 发布说明

新增：
1. XXX

实现中：
1. XXX

优化：
1. XXX

修复：
1. XXX
