# <span style="color:red;">请严格按顺序执行，防止报错、重发！</span>

## 1. nacos配置
- asset-service
```yml

```

## 2. SQL

<details>
  <summary>点击展开SQL详情</summary>

```sql
use platform_asset;

ALTER TABLE `platform_asset`.`recovery_execute`
    MODIFY COLUMN `execute_code` varchar(50) CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NULL DEFAULT NULL COMMENT '执行案号' AFTER `law_firm_id`;
-- alter table "PLATFORM_ASSET"."recovery_execute" modify "execute_code" NVARCHAR(50);
ALTER TABLE `platform_asset`.`recovery_litigation_details`
    ADD COLUMN `preservation_code` varchar(255) NULL COMMENT '保全案号' AFTER `register_id`;
```

</details>

## 3. 发业务包
- 后端asset-service.jar

## 4. 数据字典


## 5. 流程配置
### 流程
-

## 6. 重启流程引擎


## 95. 其他
- asset-service/file/template/资产导入模板.xls
- 

## 96. Nginx配置
```nginx
```

## 97. 系统菜单配置

## 98. 定时任务

## 99. 验证
- 测试（生产不可测流程）
- 代码合到master，并打tag

## 100. 发布说明

新增：
1. XXX

实现中：
1. XXX

优化：
1. XXX

修复：
1. XXX
