<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jk.asset.mapper.AgencyMapper">

  <!-- 通用查询结果列 -->
  <sql id="Base_Column_List">
    t0.id,
    t0.tenant_id,
    t0.creator,
    t0.create_stamp,
    t0.last_editor,
    t0.last_edit_stamp,
    t0.agency_name,
    t0.agency_type,
    t0.agency_nature,
    t0.contacts,
    t0.contacts_phone,
    t0.agency_address,
    t0.agency_fee,
    t0.remark
  </sql>

  <select id="findAll" resultType="com.jk.asset.model.entity.Agency">
    SELECT
    <include refid="Base_Column_List" />
    FROM agency t0
    <where>
      <if test="request.createStampRange != null and request.createStampRange.size() > 0">
        AND t0.create_stamp BETWEEN
        <foreach collection="request.createStampRange" index="index" item="item" open="" separator=" AND " close="">
          #{item}
        </foreach>
      </if>
      <if test="request.id != null">
        AND t0.id = #{request.id}
      </if>
      <if test="request.tenantId != null">
        AND t0.tenant_id = #{request.tenantId}
      </if>
      <if test="request.creator != null">
        AND t0.creator = #{request.creator}
      </if>
      <if test="request.createStamp != null">
        AND t0.create_stamp = #{request.createStamp}
      </if>
      <if test="request.lastEditor != null">
        AND t0.last_editor = #{request.lastEditor}
      </if>
      <if test="request.lastEditStamp != null">
        AND t0.last_edit_stamp = #{request.lastEditStamp}
      </if>
      <if test="request.agencyName != null">
        AND t0.agency_name LIKE CONCAT ('%',#{request.agencyName},'%')
      </if>
      <if test="request.agencyType != null">
        AND t0.agency_type = #{request.agencyType}
      </if>
      <if test="request.agencyNature != null">
        AND t0.agency_nature = #{request.agencyNature}
      </if>
      <if test="request.contacts != null">
        AND t0.contacts LIKE CONCAT ('%',#{request.contacts},'%')
      </if>
      <if test="request.contactsPhone != null">
        AND t0.contacts_phone = #{request.contactsPhone}
      </if>
      <if test="request.agencyAddress != null">
        AND t0.agency_address LIKE CONCAT ('%',#{request.agencyAddress},'%')
      </if>
      <if test="request.agencyFee != null">
        AND t0.agency_fee = #{request.agencyFee}
      </if>
      <if test="request.remark != null">
        AND t0.remark = #{request.remark}
      </if>
      <if test="request.agencyIds != null and request.agencyIds.size() > 0">
        AND t0.id in
        <foreach collection="request.agencyIds" index="index" item="item" open="(" separator="," close=")">
          #{item}
        </foreach>
      </if>
    </where>
    <choose>
      <when test="request.field != null and request.order != null">
        ORDER BY ${request.field} ${request.order}
      </when>
      <otherwise>
        ORDER BY t0.id DESC
      </otherwise>
    </choose>
  </select>
</mapper>
