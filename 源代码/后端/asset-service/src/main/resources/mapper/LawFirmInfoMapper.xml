<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jk.asset.mapper.LawFirmInfoMapper">

  <!-- 通用查询结果列 -->
  <sql id="Base_Column_List">
    t0.id,
    t0.tenant_id,
    t0.creator,
    t0.create_stamp,
    t0.last_editor,
    t0.last_edit_stamp,
    t0.org_id,
    t0.name,
    t0.contacts,
    t0.phone,
    t0.address,
    t0.remark,
    t0.enabled,
    t0.is_deleted,
    t0.due_number,
    t0.due_bank
  </sql>

  <select id="findAll" resultType="com.jk.asset.model.entity.LawFirmInfo">
    SELECT
    <include refid="Base_Column_List" />
    FROM law_firm_info t0
    <where>
      t0.is_deleted = 0
      <if test="request.createStampRange != null and request.createStampRange.size() > 0">
        AND t0.create_stamp BETWEEN
        <foreach collection="request.createStampRange" index="index" item="item" open="" separator=" AND " close="">
          #{item}
        </foreach>
      </if>
      <if test="request.id != null">
        AND t0.id = #{request.id}
      </if>
      <if test="request.tenantId != null">
        AND t0.tenant_id = #{request.tenantId}
      </if>
      <if test="request.creator != null">
        AND t0.creator = #{request.creator}
      </if>
      <if test="request.createStamp != null">
        AND t0.create_stamp = #{request.createStamp}
      </if>
      <if test="request.lastEditor != null">
        AND t0.last_editor = #{request.lastEditor}
      </if>
      <if test="request.lastEditStamp != null">
        AND t0.last_edit_stamp = #{request.lastEditStamp}
      </if>
      <if test="request.orgId != null">
        AND t0.org_id = #{request.orgId}
      </if>
      <if test="request.name != null">
        AND t0.name LIKE CONCAT('%',#{request.name},'%')
      </if>
      <if test="request.contacts != null">
        AND t0.contacts LIKE CONCAT('%',#{request.contacts},'%')
      </if>
      <if test="request.phone != null">
        AND t0.phone LIKE CONCAT('%',#{request.phone},'%')
      </if>
      <if test="request.address != null">
        AND t0.address = #{request.address}
      </if>
      <if test="request.remark != null">
        AND t0.remark = #{request.remark}
      </if>
      <if test="request.enabled != null">
        AND t0.enabled = #{request.enabled}
      </if>
      <if test="request.isDeleted != null">
        AND t0.is_delete = #{request.isDeleted}
      </if>
    </where>
    <choose>
      <when test="request.field != null and request.order != null">
        ORDER BY ${request.field} ${request.order}
      </when>
      <otherwise>
        ORDER BY t0.id DESC
      </otherwise>
    </choose>
  </select>
</mapper>
